{
  "$schema": "https://turbo.build/schema.json",
  "//": "[G6] 하네스 설정은 워크스페이스 밖에 있어 turbo 해시에 안 잡힌다 — globalDependencies 에 등록 안 하면 캐시된 '통과'가 위조된다. [실사용 발견] web#typecheck: apps/web/tsconfig.json include가 .next/types/**(typedRoutes 생성물)를 참조해, build 의존 없이 `turbo typecheck build`를 같이 돌리면 typecheck가 build보다 먼저(또는 build가 .next를 지우는 도중) 실행돼 'File not found'로 실패한다 — .harness 게이트는 체크를 별도 프로세스로 순차 실행해(build 마지막) 우연히 안 걸렸다. 근거: docs/experiment.md.",
  "globalDependencies": [
    "eslint.config.mjs",
    "eslint.harness.config.js",
    "stylelint.config.js",
    "prettier.config.js",
    "tsconfig.json",
    ".harness/config.json",
    "packages/config/**"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "web#typecheck": {
      "dependsOn": ["^typecheck", "build"]
    },
    "test": {
      "cache": false,
      "outputs": ["coverage/**"]
    },
    "db:generate": {
      "cache": false
    },
    "db:migrate": {
      "cache": false
    }
  }
}
